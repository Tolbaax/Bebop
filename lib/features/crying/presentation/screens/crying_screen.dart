import 'package:flutter/material.dart';

class CryingScreen extends StatelessWidget {
  const CryingScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Crying Screen'),
      ),
    );
  }
}
